# Email Template (MJML)

Responsive newsletter email template built with **MJML** and compiled to HTML.

## What this demonstrates
- Email layout structure (header, hero, 2-column section, CTA, footer)
- Mobile responsiveness (MJML columns stack automatically)
- Practical agency workflow: source in `src/`, compiled output in `dist/`

## Live demo
GitHub Pages: *(https://alichegini.github.io/email-template/)*

## Build
```bash
npm install
npm run build